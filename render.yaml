services:
  # Frontend (Expo app exportado para web)
  - type: static
    name: clyvo-vet-web
    buildCommand: npm install && npx expo export --platform web
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 20
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
